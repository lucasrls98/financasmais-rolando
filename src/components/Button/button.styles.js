import styled from "styled-components";

export const StylesButton = styled.button`
background-color:${(props)=> props.theme.blue700};
padding: 12px 24px;
border-radius: 4px;

`

//Styled Button é a variável e no styled.button (lib styled . tipo de tag que quero que no caso é um button)